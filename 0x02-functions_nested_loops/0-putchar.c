#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 *
 * Discription: A C program that print the last digit of a positive or negative
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
	int i;
	char str[] = "_putchar";

	for (i = 0; i < 8; i++)
	_putchar(str[i]);
	_putchar('\n');
	return (0);
}
