#include <stdio.h>

/**
 * main - Entry point
 *
 * Discription: A C program that prints the number is positive or negative
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
	int y = 48;

	while (y < 58)
	{
	putchar(x);
	if (y != '9')
	{
	putchar(',');
	putchar(' ');
	}
	y++;
	}
	putchar('\n');
	return (0);
}
