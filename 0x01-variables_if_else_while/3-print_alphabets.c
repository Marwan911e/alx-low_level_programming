#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Discription: A C program that prints the number is positive or negative
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
	int i, y;

	for (i = 97; i < 123; i++)
		putchar(i);
	for (y = 65; y < 91; y++)
		putchar(y);
	printf("\n");
	return (0);
}
